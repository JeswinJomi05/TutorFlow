# Server Configuration
PORT=5000
NODE_ENV=development

# Database Connection
MONGO_URI=mongodb://127.0.0.1:27017/tutorflow

# JWT Authentication
JWT_SECRET=tutorflow_super_secret_jwt_key_development_2026
JWT_EXPIRES_IN=7d
COOKIE_EXPIRES_IN=7

# Client / CORS
CLIENT_URL=http://localhost:5173

# Google Gemini (backend only; never add this to client/.env)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
